{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Linux Launch",
            "type": "node",
            "runtimeExecutable": "${workspaceFolder}/../../../bin/linux/DeadlyStagesDemo",
            "request": "launch",
            "cwd": "${workspaceFolder}/../../../bin/linux",
            "preLaunchTask": "rebuild"
        }
    ]
}