{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "macOS Launch",
            "type": "cppdbg",
            "program": "${workspaceFolder:macOS}/../../../build/macos-arm64-debug/DeadlyStagesRemix",
            "request": "launch",
            "cwd": "${workspaceFolder:macOS}/../../../build/macos-arm64-debug/",
            "MIMode": "lldb",
            "preLaunchTask": "compile-debug"
        }
    ]
}